name = "nextjsblog"
compatibility_date = "2024-09-23" # 建议使用较新的日期
compatibility_flags = ["nodejs_compat"] # 必须开启，Next.js 需要它

# 核心配置：告诉 Cloudflare 静态资源在哪里，入口脚本在哪里
pages_build_output_dir = ".open-next"

[site]
bucket = ".open-next/assets"

[vars]
# 如果你有环境变量，写在这里